# WSL2 deployment — worker + mcp
# Usage: docker compose -f docker-compose.yml -f docker-compose.wsl.yml up -d
#
services:
  mcp:
    networks:
      - default
      - aihelms_default

networks:
  aihelms_default:
    external: true
